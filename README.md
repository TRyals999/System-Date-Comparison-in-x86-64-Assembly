# System-Date-Comparison-in-x86-64-Assembly
This program retrieves the current system time using a Linux syscall, converts the epoch timestamp into an approximate year and month, and compares it with a user‑provided year and month. It then prints whether the user’s date is higher or equal/lower than the current system date.  This project is written in x86‑64 NASM assembly and uses the io64
